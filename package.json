{
  "name": "cackle",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "description": "Cackle — events + ticketing, offline-verifiable gate. Root package for repo-wide tooling (screenshots, third-party notices). The frontend itself lives in web/ with its own package.json.",
  "scripts": {
    "screenshots": "node scripts/screenshots.mjs",
    "demo-screenshots": "node scripts/screenshots.mjs",
    "notices": "bash scripts/gen-notices.sh"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
